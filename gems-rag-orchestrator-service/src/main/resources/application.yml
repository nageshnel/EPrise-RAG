server:
  port: 8084
spring:
  application:
    name: gems-rag-orchestrator-service
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      base-url: ${OPENAI_BASE_URL:https://openrouter.ai/api}
      chat:
        options:
          model: ${SPRING_AI_CHAT_MODEL:openrouter/free}
  cloud:
    kubernetes:
      discovery:
        enabled: true
services:
  retrieval:
    url: ${RETRIEVAL_SERVICE_URL:http://localhost:8083}
